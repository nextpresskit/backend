# Nginx: staging (nextpresskit-backend)
# Copy to /etc/nginx/sites-available/nextpresskit-backend-staging.conf and enable.
# Set APP_PORT in this environment's .env (e.g. 9091) or change proxy_pass port below.
# After editing: sudo nginx -t && sudo systemctl reload nginx
#
# SSL (Let's Encrypt): sudo certbot --nginx -d api-staging.yourdomain.com

server {
    listen 80;
    server_name api-staging.yourdomain.com;

    # Serve uploaded files directly via Nginx (recommended for production-like environments).
    # Must match MEDIA_PUBLIC_BASE_URL (default: /uploads) and MEDIA_STORAGE_DIR on the server.
    location /uploads/ {
        alias /var/www/nextpresskit-backend-staging/storage/uploads/;
        expires 30d;
        add_header Cache-Control "public, max-age=2592000";
    }

    location / {
        proxy_pass         http://127.0.0.1:9091;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }
}

