# https://editorconfig.org — keeps docs and text consistent across editors.
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

[*.md]
# Two trailing spaces can mean a hard line break in Markdown.
trim_trailing_whitespace = false
